node_modules
out
dist
.DS_Store
*.log
.env
.env.local
danh-script.db
danh-script.db-*

# Bản build đóng gói (nặng — không đẩy lên git, tự build lại bằng npm run build)
portable/
*.tsbuildinfo

# API key / cài đặt máy (an toàn — key lưu ở userData ngoài repo)
settings.json

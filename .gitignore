node_modules
out
dist
.DS_Store
*.log
.env
.env.local
danh-script.db
danh-script.db-*

# Bản build đóng gói (nặng — không đẩy lên git, tự build lại bằng npm run build)
portable/
*.tsbuildinfo

# API key / cài đặt máy (an toàn — key lưu ở userData ngoài repo)
settings.json

# Cache tool impeccable (không thuộc dự án)
.impeccable/

# Bản build tạm cho test thuần (npx esbuild ... --outfile=_handoff/.anchor.build.mjs)
# Sinh tự động từ mã nguồn — lệnh dựng lại ghi ở đầu file test tương ứng.
# (.electron-stub.mjs thì GIỮ: đó là mã viết tay, không sinh ra được.)
_handoff/.anchor.build.mjs
_handoff/.gw.build.mjs
